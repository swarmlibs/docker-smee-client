{
  "name": "smee-forwarder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf dist",
    "build": "ncc build src/server.js -o dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "smee-client": "^2.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1",
    "rimraf": "^5.0.7"
  }
}
